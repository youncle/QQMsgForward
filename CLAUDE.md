# 项目强制开发规范：OpenSpec + Superpowers 标准工作流
## 核心总则（全员/全场景强制执行）
1. 本项目所有代码变更、功能迭代、接口改造、逻辑优化，**必须严格走完整标准化工作流**，禁止跳过环节、禁止无序开发。
2. 所有流程以「文档为唯一依据」，代码必须对齐规格文档，无文档不开发、无验收标准不合并、无校验不归档。
3. 严格遵循 Superpowers 官方技能准则 + OpenSpec 规格管控，所有环节为硬性门禁，非特殊禁止豁免。

## 固定标准工作流（不可调整顺序）
需求澄清 → 头脑风暴（brainstorming）→ 规格提案（/opsx:propose）→ 方案规划（writing-plans 产出 plan.md）→ TDD开发/子代理并行执行 → 全维度验证（verification）→ 代码审查（code-reviewer）→ 规格校验（/opsx:verify）→ 流程归档（/opsx:archive）

## 流程豁免规则（仅以下场景可豁免）
1. 纯文案修改、注释微调、格式排版，无需走流程
2. 单行配置修复、无逻辑变更的参数微调
3. 紧急线上Hotfix（修复后必须补全规格文档与复盘记录）

## 各环节硬性门禁
### 1. 需求&设计门禁
- 必须先完成多方案 brainstorm 对比，明确最优方案后，再生成规格提案
- 所有模糊需求必须逐一提问落地，无歧义、无遗漏、无待定项

### 2. 方案规划门禁（plan.md 管控）
- 多任务、多文件、多模块改动必须产出 plan.md
- 必须明确阶段拆分、串行/并行策略、子代理分配、阶段检查点
- 所有依赖关系必须显性写清，禁止无序并行开发

### 3. 任务拆解门禁
- 所有开发任务必须写入 tasks.md，每条任务必须附带【验收契约】
- 任务必须明确：文件路径、操作内容、执行标准、完成判定条件

### 4. 开发执行门禁
- 严格遵循 TDD 准则：**无失败测试，不写业务代码**
- 强制 RED-GREEN-REFACTOR 循环，先写失败用例、再实现代码、最后重构优化
- 子代理仅负责执行产出，禁止擅自修改任务清单、规格文档、规划文档

### 5. 验证&审查门禁
- 开发完成必须执行全量验证：单元测试、代码规范、编译构建、业务逻辑校验
- 所有 P0/P1 级问题必须100%修复，禁止遗留风险问题

### 6. 归档门禁
- 归档前必须通过 /opsx:verify 规格校验
- 所有任务必须全部完成勾选，文档、代码、测试、日志完全对齐

## 质量红线
- 禁止口头需求落地、禁止边想边写、禁止先写代码后补文档
- 禁止跳过测试开发业务逻辑
- 禁止规格文档与最终代码不一致