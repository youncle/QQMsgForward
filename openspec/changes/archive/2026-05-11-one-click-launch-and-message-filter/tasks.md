## 1. 基础设施

- [x] 1.1 创建 `config.json` 配置文件，包含所有可配置项（转发规则、过滤规则、连接参数）
- [x] 1.2 创建 `requirements.txt`（flask, requests, pystray, pillow）
- [x] 1.3 修改 `qq-message-forward.py` 从 `config.json` 读取配置，替换硬编码变量

## 2. 消息过滤模块

- [x] 2.1 创建 `filter.py`，实现 `check_qrcode_ad()`（图片段+关键词检测）
- [x] 2.2 在 `filter.py` 中实现 `check_contact_info()`（正则匹配联系方式）
- [x] 2.3 在 `qq-message-forward.py` 中接入过滤模块，在转发前执行过滤检查
- [x] 2.4 实现 `log_only` 模式（仅记录日志不拦截）和正常拦截模式

## 3. 一键启动脚本

- [x] 3.1 创建 `start.vbs`，实现 UAC 自动提权、LLBot 静默启动、端口检测、托盘程序启动
- [x] 3.2 创建 `stop.vbs`，实现 UAC 自动提权、所有相关进程强制终止

## 4. 系统托盘程序

- [x] 4.1 创建 `tray.py`，实现托盘图标创建、右键菜单（查看状态 / 关闭服务）
- [x] 4.2 实现服务状态监控（定时检测 LLBot 和转发脚本是否在运行）
- [x] 4.3 实现"查看状态"弹窗（显示各服务运行状态、转发规则摘要）
- [x] 4.4 实现"关闭服务"功能（终止所有子进程、移除托盘图标、可选关闭 LLBot）

## 5. 清理与验证

- [x] 5.1 更新 `使用教程.txt`，添加一键启动说明和过滤配置说明
- [x] 5.2 端到端测试：启动 → 验证托盘图标 → 发测试消息验证过滤 → 关闭 → 确认进程清理
