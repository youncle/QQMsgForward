## 1. 图标绘制重写

- [x] 1.1 重写 `create_icon_image()` 函数：将绘图逻辑从聊天气泡改为中性色圆点 + 彩色外圈（外圈 3px 宽，圆点 #37474F，外网使用 colors 字典中定义的颜色）
- [x] 1.2 验证三种状态颜色（绿/红/黄）各自正确显示，确认圆点颜色恒定为深灰色不变
- [x] 1.3 启动程序，在系统托盘中目视确认图标在各缩放尺寸下显示正常，状态切换时外圈颜色正确变化
- [x] 1.4 程序启动时生成 `.ico` 图标文件，更新桌面快捷方式 `create_desktop_shortcut()` 设置 `sc.IconLocation`
